# CODER.md

> Project conventions for AI coding assistants. All agents (Claude, Kilo Code,
> Qwen, or anything else) should read this file first and treat its
> rules as binding for every task in this repo.
>
> Edit **this one file** when conventions change — every provider's native
> memory file already points here via `@CODER.md`.

_Generated by MR Multi Model Ai Coder on 2026-08-10._

---

## Hard rules

> Non-negotiable for every task in this repo, regardless of the conventions
> captured below. Get explicit owner approval before deviating.
>
> **Scope of the rename rule:** it covers every identifier that already exists —
> variables, constants, functions, classes, methods, exports, route paths, DB
> columns, and config keys. A rename silently breaks callers, imports, persisted
> data, and public APIs this repo cannot see.
>
> **On tests:** state in one line what behaviour the test protects and why it
> needs to exist *before* writing it, then check the observed result against that
> stated expectation before calling it done. This applies to unit, integration,
> E2E, and automation tests alike.
>
> **On passwords:** a leaked secret is not un-leaked by deleting it — rotate any
> credential that was ever committed.
>
> **On the review pass:** it is a re-read of the work with fresh eyes — re-check
> the current session's tasks *and* what earlier sessions changed, looking for
> regressions, half-finished edits, and rules broken elsewhere. Report what you
> find; do not silently "fix" beyond the task's scope.

- **NEVER rename existing variables, constants, functions, or exports** without explicit owner approval. Add new ones alongside if needed — never change existing names. Renames break callers, imports, persisted data, and public APIs.
- **Name new functions and variables meaningfully** — intention-revealing (`resolveActiveSubscription`, not `handle2` or `tmp`). No cryptic abbreviations, no single letters outside loop indices, no numeric suffix to dodge a collision.
- **Tests assert real expectations, not liveness** — assert the actual outcome: returned values, persisted state, rendered output, or events. Asserting only `status === 200`, no-throw, or truthy is not a test; it passes while the feature is broken.
- **Passwords are hashed and at least 32 characters** — 32+ chars, stored only as a salted hash (bcrypt / argon2 / scrypt). Never store, log, echo, hard-code, commit, or return a plaintext password.
- **Never modify files outside the workspace root you were opened in.** No sibling checkout, parent directory, or other repository — those edits sit outside this repo's history and never reach code review. Need one? Stop and ask.
- **Mandatory review every turn** — after each completed turn, re-review this session's tasks AND earlier sessions' before moving on. Every fix and new implementation gets its own review pass. Costs extra tokens per turn; the owner opted in.

---

## Engineering rules

- **CRITICAL — `DEMO_STEP_BY_STEP.md` IS OWNER-OWNED. NEVER EDIT IT WITHOUT ASKING FIRST.**
  That file is the specification the product is judged against, not working notes.
  It is written and owned by the owner; agents READ it and IMPLEMENT it.

  | Forbidden without explicit owner approval | Includes |
  |---|---|
  | Editing any existing line | Rewording, "clarifying", fixing a typo, tightening a table row |
  | Adding a new step, phase, rule, or section | New §x.y, a new row in any rule table, a new branch in any flow diagram |
  | Deleting or reordering anything | Removing a rule that looks obsolete, resequencing sections |
  | Making the doc match the code | Editing the spec so a divergence disappears — see below |

  **When the code and the doc disagree, the DOC is right and the CODE is the
  bug.** Never "fix" the disagreement by editing the doc. Report the divergence,
  quote the exact lines, propose the change, and WAIT for a yes.

  Asking means: state what you want to change, show the before/after, and get an
  explicit approval for THAT change. A general instruction — "fix everything",
  "address all points", "go ahead" — is NOT approval to edit this file. Neither
  is the owner approving the BEHAVIOUR the change describes. If in doubt, ship
  the code and leave the doc alone.

  Only after an explicit yes: make the smallest edit that records the decision,
  and say exactly which lines changed.

- **Language**: Use TypeScript in strict mode only — no `.js` / `.jsx` in application code.
- **Icons**: SVG only for icons and illustrations — no emojis in product UI.
- **DOM IDs**: Every DOM element must carry a module-specific prefixed ID (e.g. `{module}_{component}_{descriptor}`).
- **Re-renders**: No unnecessary component re-renders — memoise, split, subscribe narrowly on every component.
- **Modules**: Strict module separation — every feature has its own folder, routes, and tests.
- **Async**: No `setInterval`, no `setTimeout` for polling. Everything is event-driven: event bus, WebSocket push, AbortController, etc.
- **Logging**: All errors logged in OTEL-structured format. Never `console.error` alone; never swallow errors.
  - Server side: import and call `logOtel(component, event, level, payload)` from `@/lib/logging/otel`. The canonical helper injects `timestamp` and writes JSON to stdout. For sites that already have the payload as an object literal, use `logOtelEntry(payload)` instead — same output, one-line call.
  - Client side: import and call `logOtelClient(component, event, level, payload)` from `@/lib/logging/otel-client`, or `logOtelClientEntry(payload)` for the one-line form. The client helper picks `console.error/warn/info` by level; an installed global error reporter in the root layout ships browser-side errors to the server via `POST /api/logs/error`.
  - Do not inline `console.X(JSON.stringify({level, component, event, ...}))` in either layer — every entry MUST carry the three required fields (`level`, `component`, `event`), and the canonical helpers enforce this. The shorthand exists for the rare case where restructuring the call site is impractical.
  - Never swallow an error: every catch block MUST either re-throw, return a typed error to the caller, OR emit an OTEL entry via the helpers above. Bare `catch { /* ignore */ }` is forbidden.
- **E2E policy**: Every change ships with E2E coverage run in headed mode.
  - **Test the real product, never a test-only backdoor.** The app MUST NOT contain any event, hook, seam, window handle, query-param gate, or route that exists only to make E2E possible (e.g. a `?e2e=1` window seam that fires internal callbacks, or `/api/e2e/*` routes). Those are production code paths a real user can reach, and faking internal events means the test proves nothing about the real flow. Drive the actual UI a prospect drives: click the real buttons, type in the real composer, let the real WebSocket/voice pipeline run. If a behaviour genuinely cannot be exercised through the real UI (e.g. live-mic voice barge-in), do NOT add an app seam to fake it — leave it to manual/live verification and say so.
  - **One config, v2 routes only.** There is a SINGLE Playwright config at the repo root (`playwright.config.ts`). Do NOT add per-feature `playwright.<name>.config.ts` files. Every spec targets the v2 demo route only — `/v2/[workspace]/demo/[id]` — never legacy `/default/demo` or `/[workspace]/demo` paths.
  - Seeding test data directly against Postgres from a test fixture is fine (it's test-side, not app code); shipping a data-mutating HTTP route inside the app for tests is not.
- **Verification**: Always run a production build to verify changes. Never ship on dev-only checks.
- **CRITICAL — NEVER SAY "FIXED"/"DONE"/"COMPLETE" WITHOUT TRACING IT FIRST.**
  Saying a thing is done is a CLAIM. Verify the claim before making it, not after
  the owner questions it. Repeatedly announcing a fix and then discovering it was
  partial is the single most expensive failure mode in this repo — it burns the
  owner's trust and their time.

  Before the words "fixed", "done", "complete", "working", or "verified" are
  written, ALL of these must be true:

  | # | Must be true | How to check |
  |---|---|---|
  | 1 | The flag you set is actually READ by the thing you meant to stop | Grep every read. A boolean nothing consumes is a comment, not a brake. (`clientStickyPause` stops `resumeTour()` — it does NOT stop a tour already running; that needs `pauseTour()`.) |
  | 2 | The event you emit has a SUBSCRIBER | Grep for the listener. (`waiting_for_answer` has none — it is a marker, not a brake.) |
  | 3 | EVERY path to the bug is closed, not just the one you found | Server AND client, tool AND timer, LLM speech AND hub speech. |
  | 4 | It RAN, in a real session, and you saw the evidence | A green build is not a run. A skipped test is not a pass. Quote the log line or say "not yet executed". |
  | 5 | The state you rely on survives the lifecycle | One-shots that never reset; flags cleared on reconnect; timers re-armed. |

  If any is unmet, say what IS done and what is NOT, in the same breath — never
  "fixed" with the caveat buried later. Preferred wording: "implemented, not yet
  executed" / "holds X, does not yet hold Y".

  **No "partly".** A half-enforced rule is an unenforced rule: if the doc says the
  agent MUST do something, the code must CHECK it, not merely prompt for it. A
  rule the code does not check is a suggestion. Close it or state it as an open
  gap with a plan — never ship it as "partly done".

- **Post-implementation review (MANDATORY after EVERY fix or implementation)**: a
  change is not done when it compiles — it is done when it has been reviewed
  against the checklist below and the findings reported. Run this on your OWN
  work, before saying it is finished, and state the result explicitly.

  | # | Check | Fails when |
  |---|---|---|
  | 1 | **Right primitive?** | The call does something adjacent to what was needed — e.g. `dispatchAndRespond` for a call Deepgram never issued, or the lead-capture card (`ask_user_question`) used to ask a conversational question in a VOICE demo. Adjacent is not correct. |
  | 2 | **Right modality?** | A voice agent posts a form; a spoken flow requires typing; a visual change is announced but never shown. Match the channel the prospect is actually using. |
  | 3 | **Root cause, or one of several paths?** | The same bug is reachable from another route (server AND client, tool AND timer). Fixing one path and declaring victory is the most common failure here — grep for every caller before closing. |
  | 4 | **Idempotent / one-shot where it must be?** | A re-arming timer stacks cards, a reconnect re-fires a one-shot, a retry double-charges an LLM turn. |
  | 5 | **Wording factually true in EVERY state it can fire?** | A line reused across situations that claims something false in one of them ("That's the full walkthrough" mid-walkthrough). |
  | 6 | **Observable when it fails?** | Every branch — including the silent fallback — emits OTEL via the canonical helpers. No bare `catch {}`, no `.catch(() => {})`. |
  | 7 | **Rules honoured?** | No renamed existing names, no inline LLM-facing strings outside `prompts.ts`, no new events/vars when the vocabulary is closed, `EVENTS_AND_HOOKS.md` updated for real events/frames/hooks. **`DEMO_STEP_BY_STEP.md` untouched unless the owner explicitly approved that specific edit** (see the CRITICAL rule above) — if this change made the code diverge from the spec, report it, do not edit the spec. |
  | 8 | **Proven, or merely written?** | Say plainly which lines have actually EXECUTED in a run and which have only been typed. "Written but never executed" is not done. |
  | 9 | **Test measures the documented outcome?** | The assertion counts a proxy rather than the thing (toggle frames counted as turns; a client-only marker looked up in server logs). A green test proves nothing if it measures the wrong quantity. |
  | 10 | **Production build run?** | `npx next build --webpack` after the change, not just `tsc --noEmit`. |

  Report the outcome honestly, including checks that FAILED and were fixed as a
  result. A review that never finds anything is a review that was not done.

---

## 1. Project identity

Ai Sales agent, the Al Driven demo planform which strictly follow the event driven architecture and no timeouts

### Workspace boundary

- **Workspace root**: `c:/Users/Public/Projects/Sales Engineer Ai Agent/sales-engineer-ai`
- **Work only inside this folder.** Every file you read, create, edit, move, or delete must resolve to a path under the workspace root above. This is the whole project as far as any task is concerned.
- **Never touch another project.** Do not edit a sibling checkout, a parent directory, another repository, or a folder left open from earlier work — even when it looks related, shares a name, or would "fix" the problem faster. Those edits are outside this repo's git history, so they never appear in review or in `git status` here.
- **Relative paths resolve from the workspace root**, not from your shell's current directory. Prefer workspace-relative paths in commands so a stray `cd` cannot silently retarget a write.
- **If a task genuinely needs a file outside the root** — a shared library, a sibling service, a global config — stop and ask. Say which path and why. Do not proceed on the assumption that access implies permission.
- **Exception**: read-only inspection of tool-managed locations the workflow depends on (package manager caches, `~/.gitconfig`, CLI credential dirs) is fine. Writing to them is not.

## 2. Tech stack & versions

Node.js, docker, postgress, NextJs,tilewind, prisma

## 3. Styling framework

- **Framework**: Tailwind CSS
- **Working rule**: Use utility classes. Define design tokens in `tailwind.config.ts`; no raw hex values in JSX.

## 4. Directory structure

- **Methodology**: Domain-driven
- **Working rule**: One folder per bounded context. Inter-context communication via events only.

## 5. Coding conventions

_(not specified — match the style of a nearby existing file)_

## 6. Architecture rules & patterns

- Modules with Event Buz

## 7. Don'ts / forbidden patterns

- **Always use meaningful names.** Every function, variable, constant, useState hook, and type — new OR existing — must describe what it does or holds. No single-letter names (except loop indices), no `temp`, `data`, `val`, `res`, `obj`, `item`, `thing`, `stuff`, `state`, `index`, `done`, `busy`, `current`, `entries`, `error` (without a subject prefix). The name must answer "of what?" / "by whom?" without reading surrounding code. Booleans must state the subject: `isAgentSpeaking` not `isSpeaking`, `allQuestionsAnswered` not `done`, `isSubmittingAnswer` not `busy`. If an existing name is vague or ambiguous, rename it in place (update all callers) — meaningful names are more important than name stability.
- **Events & hooks: single source of truth.** All event names, WebSocket frame types, bus events, and hook responsibilities are catalogued in [`EVENTS_AND_HOOKS.md`](EVENTS_AND_HOOKS.md). Before adding a new event or hook, check that file first. After adding one, update that file. Never invent ad-hoc event names that aren't in the catalogue. For state-toggle patterns (set/get/clear), consolidate into dedicated methods (e.g. `setAgentSpeaking()` / `clearAgentSpeaking()` / `isAgentSpeaking()`) — no scattered inline assignments.

## 8. Domain glossary

Ai, Demo Plaform, Sales agent

## 9. External services & secrets

Deepgram, OpenAi, chatGPT, mr-coder.io

## 10. Additional notes

_(none)_

---

## Testing & CI

- **Unit / integration runner**: Vitest
- **E2E tool**: Playwright

## E2E Testing Rules

- **Enumerate before you write**: open every in-scope page and list every interactive element *before* the first test. Specs written from memory or a screenshot miss conditionally-rendered and nested controls.
- **Walk nested structure to the leaf**: menu → submenu → sub-submenu, every dropdown's full option list, every conditional field that appears only after another is filled. "The dropdown opens" is not coverage.
- **Flag unknowns, never guess**: if an element's expected behaviour isn't clear from the UI, mark it `UNKNOWN — needs confirmation` and ask. An invented assertion produces false-positive coverage, which is worse than a gap.
- **Locator priority**: `getByRole` > `getByLabel` > `getByTestId` > `getByText` (exact) > CSS. Never use auto-generated class names or index-based selectors as the primary locator — they break on every refactor.
- **One element, one stable locator**: scope ambiguous matches to their container (`row.getByRole('button', { name: 'Edit' })`). Repeatedly needing `.nth()` is a signal to add a `data-testid`, not to index harder.
- **Buttons**: assert enabled/disabled state for the context, the click's real effect, that a disabled button does nothing, that async clicks show a pending state until they resolve, and that destructive actions confirm first.
- **Forms**: cover default state, valid submit (assert the success signal), required-field validation on empty submit, per-field format validation, error clearing on correction, and conditional field show/hide.
- **Forms, reset paths**: if a reset or cancel control exists, assert it clears the fields and any validation errors — a half-cleared form is a common regression that a submit-only test never catches.
- **Dropdowns**: assert the *full* option list, not a count. Selecting an option updates the display value and triggers dependent behaviour. Verify keyboard open/select and close on Escape and outside click.
- **Links**: assert the resolved target, not just presence. Internal links land on the right route; `target="_blank"` links must carry `rel="noopener noreferrer"` — treat a missing rel as a security failure.
- **Links, inactive state**: a disabled or current-page link (breadcrumb, active nav item) must not navigate. Assert the URL is unchanged after clicking it, rather than assuming the styling implies the behaviour.
- **Menus**: locate items structurally (parent → child chain), never by absolute position or timing. Assert each item's destination, that the active route is marked via class/attribute, and that menus close on select and outside click.
- **Filters**: assert the actual filtered result set, not "the list changed". Cover each control alone, a multi-filter combination, clear-one, clear-all, the empty-result state, and URL/query-param deep links.
- **Filters, persistence**: if filter state is meant to survive pagination or a tab switch, assert it actually does. Silent filter reset on page 2 is a defect users hit constantly and specs rarely cover.
- **Search**: cover the empty query, a matching query, a no-match empty state, whitespace-only and very long input, and that clearing the box restores the prior list.
- **Search, request volume**: if the input is debounced or throttled, assert the request count for a burst of typing. A debounce that silently stops working is invisible to result-only assertions.
- **Tables**: assert row count and key cell data, sorting per column in both directions, pagination boundaries (first/last page), and every row-level action independently.
- **Modals**: assert focus moves into the dialog on open, its action buttons perform the real operation and close (or stay open) as designed, and it closes via *each* of button, Escape, and backdrop click.
- **Toasts**: assert the correct message text appears for both success and error paths, and that it either auto-dismisses within its designed window or can be dismissed manually — whichever the design specifies.
- **One spec per journey, not per element**: name the file for the flow (`checkout.spec.ts`), group sub-features with `describe` blocks, and keep each test to one logical behaviour so a failure names its own cause.
- **No inter-test dependencies**: use fixtures for auth state and seeded data. No test may rely on execution order or on state a previous test left behind.
- **Negative paths are mandatory**: every form needs an invalid-submit test, every filtered list an empty-result test, every destructive action a cancel test, and every async call a simulated failure via `page.route()`.
- **Never use `waitForTimeout`**: fixed sleeps are the main source of flaky suites. Use web-first assertions (`toBeVisible`, `toHaveText`), or wait on the specific network response the UI depends on.
- **Engine-agnostic assertions**: specs must pass on every configured browser project (chromium, firefox, webkit). Don't rely on engine-specific timing, focus order, or CSS behaviour that only holds in one of them.
- **Fail on console errors**: attach console and uncaught-exception listeners in a shared fixture before navigation, and assert both are empty at test end. A control that renders correctly while throwing is not passing.
- **Fail on unexpected network errors**: record responses with status >= 400 and assert none occurred on a happy path. A negative-path test asserts its *specific* expected status — any other 4xx/5xx still fails.
- **Allow-list errors explicitly, never silently**: a known-noisy error needs a documented reason and a linked ticket in a shared allow-list file. Blanket suppression turns a real regression into a green run.
- **Check server logs for the test window**: where logs are reachable, assert no ERROR/FATAL lines were emitted by the test's actions. If they aren't reachable in this environment, record that as a stated coverage gap.
- **Emit a per-run error report**: write `{ test, consoleErrors, networkErrors, serverErrors }` per test as a CI artifact. A pass/fail boolean hides a suite that is going green while getting noisier every week.
- **Automated a11y scan per page**: run an axe-core scan (`@axe-core/playwright` or `cypress-axe`) in the shared fixture and fail on violations. It catches contrast, missing labels, bad ARIA, and focus traps that manual review misses.
- **Keyboard-only path**: for each primary journey, complete it using only Tab / Shift+Tab / Enter / Escape. Assert focus order is logical and no control is reachable by mouse alone — this is the most common a11y regression.
- **Visual regression on stable views**: snapshot each key page and component state (`toHaveScreenshot` / equivalent) so unintended layout and style changes fail automatically instead of waiting for someone to notice.
- **Mask the volatile parts of snapshots**: exclude timestamps, avatars, random IDs, and animations from visual comparisons. An un-masked snapshot fails on every run, and a test that always fails gets deleted.
- **Test the responsive breakpoints you ship**: run the critical journeys at mobile, tablet, and desktop widths. A desktop-only suite cannot catch a nav that collapses wrongly or a control pushed off-screen.
- **Definition of done**: every enumerated element maps to at least one assertion, negative cases exist, no fixed sleeps remain, error assertions run via shared fixture, and the spec header notes coverage plus open UNKNOWNs.

## Git & workflow

- **Commits**: Use Conventional Commits: `feat:`, `fix:`, `refactor:`, `chore:`, `docs:`. One logical change per commit.

## API conventions

- **Style**: REST — resource-oriented endpoints. Use proper HTTP verbs and status codes.

## Deployment

- **Target**: Docker / Docker Compose. Ensure Dockerfile is optimised (multi-stage builds, layer caching).

### Local servers & ports (IMPORTANT)

The app runs a **custom server** (`server.ts` via `tsx`), not bare `next`, so the
port comes from the `PORT` env (NOT the `-p` flag). Keep dev and prod on
**separate ports** so a hot-reloading dev server never clobbers the production
build that the Cloudflare tunnel serves.

- **Dev → 3444**: `npm run dev` (forces `PORT=3444`, `tsx watch`, hot reload) with SSL enabled.
  NEVER run the dev server on 3443.
- **Production → 3443**: `npm start` (or `PORT=3443 npm start`). This is what
  the tunnel `ai-sales-agent.mr-coder.io` serves. Build first:
  `npx next build --webpack` (webpack, not turbopack), then `npm start`.
- **Production (self-healing) → 3443**: `PORT=3443 npm run start:guard`. Wraps
  the server in `scripts/prod-supervisor.mjs`, which respawns it (capped
  backoff) if the process is killed — a `kill-port 3443`, a redeploy, or a
  crash no longer takes the public demo offline. Prefer this for the live
  tunnel; plain `npm start` is fine for one-off local runs.
- To free a port, use `npx kill-port <port>` — never "kill all node processes".
- For isolated test servers, override the port: `PORT=4005 npm start`.

---

## Behaviour preferences

- **Max file size**: 750 lines — split the file before exceeding it.
- **When a task is ambiguous**: Ask a clarifying question before proceeding. Do not silently guess.
- **Secrets**: never commit `.env`, API keys, credentials, or anything under the off-limits paths in §9.
- **Commands**: read the project's `package.json` / `Makefile` / CI config to discover the real dev / build / test / lint commands — never guess.
- **Reading before editing**: read the full file (or the containing module) to understand context — never patch blind.
- **Matching existing style**: read a nearby file first; match its naming, imports, and error handling.
- **Commit / summary messages**: describe the *why*, not the *what* — the diff already shows the what.
- **Dev Port**: 3444 with SSL
- **Production Port**: 3443 DOCKER, never kill 3443 as this is with docker now, you can use npm run build:prod:restart to restart the docker 3443 port
- **Do not kill all node processes**: if you wanted to kill a port, use `npx kill-port {port number}` command
- **Do not run `git chekout`**: you should not use `git checkout` command even there are un committed changes.